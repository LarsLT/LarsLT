# Claude Code — track shared config, ignore personal/in-flight stuff
.claude/settings.local.json

# Generated SVGs are published to their own branch, never committed to main
dist/

# Last good upstream responses, kept only so a rate-limited run still draws
cache/

# Compiled generators, in case someone builds instead of go run
/tools/space-map/space-map
/tools/space-map/cmd/*/buildbasemap
/tools/space-map/cmd/*/buildeclipses
