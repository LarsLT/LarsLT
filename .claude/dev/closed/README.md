# dev/closed/

Work that has **landed**: implemented, PR reviewed, and merged. Moved here from
`dev/active/` once that's true.

Kept as institutional memory — why we did it, what we rejected. Tracked in git.

Before moving a folder here, promote anything with lasting value (conventions,
architecture, gotchas) into `.claude/docs/` so it's found without digging.

See `../README.md` for the full lifecycle.
